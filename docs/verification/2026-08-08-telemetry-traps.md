# 텔레메트리 지표 실측 — 2026-08-08

> **오픈북 계층 문서.** 여기는 측정 결과(원재료)만 둔다. 여기서 도출된 **규칙은 허브가 소유한다** —
> `docs/_hub/nodes/C-metric-*`. 규칙을 이 문서에 중복으로 두면 다시 대장이 되므로 두지 않는다.
> 노드 `S-review-sim-20260808`이 이 파일을 근거 실물로 가리킨다.

## 배경

`amendment #5`(예측·판정 루프) 설계에서 플레이어 습관 지표 5종과 임계값을 정했다. 임계는
부모 스펙 §3.4의 로그 **예시 값**(`wallHugRatio: 0.55`, `hotspotConcentration: 0.31`, `dashCount: 6`)에서
가져왔는데, 그 값들은 문서를 쓰며 지어낸 것이지 실측이 아니었다.

독립 검토 2종이 각각 `src/telemetry/collector.ts`를 재구현해 시뮬레이션했다. **코드 읽기로는 잡히지
않던 결함이 전부 여기서 드러났다** — 5종 중 4종이 플레이어의 습관이 아니라 아레나의 기하나
웨이브 길이를 재고 있었다.

## 측정 조건

아레나 960×640 · `WALL_MARGIN=80` · 히트맵 `HOT_COLS=8 × HOT_ROWS=6` · 플레이어 이속 220 ·
chaser 90 · shooter 유지거리 260 · 45초 웨이브 기준. 플레이어 모델은 이상화(완벽한 도주·키팅)라
실제 값은 더 극단적으로 나온다.

## 궤적별 지표

| 궤적 | `max(quadrantTime)` | `wallHugRatio` | `hotspotConcentration` | 임계 초과 |
|---|---|---|---|---|
| 원 궤도 키팅 r=200 | 0.25 | 0.00 | 0.10 | **없음** |
| 원 궤도 키팅 r=270 | 0.26 | 0.31 | 0.08 | **없음** |
| 외곽 순찰 (40px 안쪽) | 0.29 | 1.00 | 0.06 | WALL |
| 구석 캠핑 ±60px | 1.00 | 0.00 | 0.49 | CORNER, ANCHOR |
| 우측 절반만 (용암 회피) | 0.51 | 0.00 | 0.15 | CORNER |

**유능한 키팅 플레이어에게는 아무것도 걸리지 않는다.** 런당 예측이 0회일 수 있다는 뜻이다.

## `wallHugRatio` — 궤적별

도주 0.88~0.94 · 키팅 0.62 · **의식적 중앙 유지 0.50~0.63** · 정지 0.00.

밴드 면적 산술(직접 검증): `1 − (960−160)(640−160)/(960×640) = 1 − 384000/614400 = 0.375`.

## 웨이브 길이 의존성

동일 궤적, 웨이브 길이만 변경:

| 길이 | `max(quadrantTime)` | `hotspotConcentration` |
|---|---|---|
| 15초 | 0.89 | 0.53 |
| 30초 | 0.45 | 0.26 |

실측 전멸 시간 범위는 3.6~29초(`2026-08-06-budget-curve.md`), 플레이어 초당 피해는 런 중 12배 성장.

## `clusterRatio`

18개 (구성 × 전략) 조합 중 **16개가 임계 0.25 미만**. 범위 0.05~0.24. 정지 상태에서도 미만.
임계를 넘긴 유일한 경우는 `shooter` 단독 ×10 → 0.38(구성은 디렉터의 선택).

적을 흩으려 2초마다 방향을 뒤집는 플레이어 모델: **0.11 → 0.05**, **0.15 → 0.06**으로 하락.

## `dashCount` 구조적 초과율

기본 쿨다운 2000ms 기준, 임계 8 대비 초과율: 45초 웨이브 **1.75** · 70초 **3.38** ·
`DASH_CD_DOWN` 3회 강화 시 **7.50**. 비교 대상 지표의 구조적 상한은 CORNER 1.50 · WALL 1.00 ·
HERD 1.00 · ANCHOR 1.86.

## 한계

시뮬레이션 스크립트는 세션 스크래치패드에 있어 **휘발됐다.** 위 수치는 리뷰 산출물에서 옮긴 것이며
원 스크립트로 재현할 수 없다. 재검증이 필요하면 `collector.ts`를 다시 떼어내 돌려야 한다.
직접 산술로 검증한 것은 밴드 면적(0.375)과 격자 내포 관계(8×6 → 사분면당 4×3) 둘뿐이다.

## Phaser 측정 (별개 계열, 같은 날)

- `Clock.js:370` = `this.now = time` — `timeScale` 미적용. `:377`에서 TimerEvent `delta`에만 곱해진다.
- `World.js:947` = `msPerFrame = _frameTimeMS × timeScale`, `fixedStep` 기본 true → **역수 관계**.
- 백그라운드 탭에서 rAF 스로틀로 FPS 14 관측 — 스크린샷 기반 검증 시 타이머가 거의 안 돈다.
