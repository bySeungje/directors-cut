# 관찰하는 아레나 — 예고가 먼저 나오는 AI 대결 (가제 1983)

> 작성일: 2026-08-09 · 작성자: byseungje · 상태: frozen

## 1. 목적
NAN 2026 사전과제 제출물의 최종 방향이다. 마감은 2026-08-11 00:00로 확정됐고(공고 사이트 카운트다운 직접 관측) 현재 8/10 08:50, 폴백 제출물이 없다. 승제가 말한 이 게임의 씨앗은 하나다 — '아주 단순했던 추억 게임에 진짜 AI가 나를 공략하려고 한다.' 그 추억 게임은 승제 기준으로 초기 아레나(무채색 도형·미사일·웨이브)다. 따라서 몸통은 아레나를 그대로 살리고 그 위에 씨앗이 요구하는 감정(소름)을 만드는 층만 얹는다. 결정 문서의 선택지 이름은 '신작 착수'이지만, 승제가 심의 이후 '아레나도 내 기준에서는 추억게임이었다. 그걸 그대로 살려라'라고 방향을 좁혔으므로 이 스펙은 아레나 몸통 위의 신작으로 확정한다 — 씬과 규칙 층은 새로 쓰고 계약·디렉터·검증기·폴백·텔레메트리·하네스는 재사용한다는 그 옵션의 정의와 일치한다. 해결할 것은 넷이다. (1) 아레나가 '여전히 너무 쉬워'로 폐기된 문서화된 원인은 밸런스가 아니라 입력 설계였다 — 자동 조준·자동 사격이라 빗나갈 수 없고 이속 우위라 잡힐 수 없어 플레이어가 부족해질 수 있는 축이 없었다. (2) AI가 플레이어를 읽는다는 핵심 약속이 10px 로그로 2.6초 스쳐 지나가 심사자가 30초 안에 볼 수 없다. (3) 'AI가 당신에게 적응한다'는 이 대회에서 가장 흔할 컨셉이라 그것만으로는 차별화가 되지 않는다. (4) 소름은 관찰과 재배치를 구현했다고 나오지 않는다 — 예고가 실행보다 먼저 나올 때만 난다. 이 순서가 이 게임의 유일한 성공 조건이다.

## 2. 현재 컨텍스트 요약
sj-team 다역할 심의 산출물에서 생성됨 — run_id: nan-2026-2026-08-11-00-0-eb8ad4 · 채택안: opt-new-game-gated · source_decision_hash: sha256:c5956367f19e4c393bfb0cf5bb20377dbc0e57f1f6964c1d6abd6ba2432906bc · provenance_ref: agent:spec.
(대상 코드베이스의 관련 코드/패턴 탐색은 /dlc Phase 1에서 수행한다.)

## 3. 변경 범위
- [must] req-goosebump-sequence: 소름 연출 순서를 다섯 비트로 고정한다. (1) 웨이브 중 — 습관 미터가 차오른다(뭔가 재고 있다는 불안). (2) 웨이브 종료 즉시 — 관찰: '너는 왼쪽 3분의 1에서 82% 머물렀다'. (3) 1.2초 뒤 — 예고: '그래서 왼쪽을 닫는다'. 이 시점에 아직 아무 일도 일어나지 않는다. 여기가 소름의 자리다. (4) 다음 웨이브 시작 — 예고한 쪽에 스폰 마커가 0.6초 먼저 그려지고 그 자리에서 적이 나온다. 말이 먼저, 그림이 나중. (5) 웨이브 종료 — 예고와 판정을 나란히 놓고 '말한 대로 됐다' 또는 '너는 빠져나갔다'를 보여준다. 3번과 4번의 순서가 뒤집히면 같은 코드로도 소름이 나지 않는다. 구현 순서도 이 파이프라인(예고 → 마커 → 스폰 타이밍)을 관찰 문장 UI보다 먼저 만든다. 체류 시간 1.2초·0.6초는 실플레이 판정에서 조절하는 유일한 레버다.
- [must] req-first-observation-midwave: 첫 관찰을 웨이브 종료에 매달지 않는다. 웨이브 1의 12초 지점에서 게임을 0.8초 정지시키고 화면 중앙에 첫 관찰 문장과 이동 히트맵을 겹쳐 띄운다. 습관 측정 창이 이미 12초 단위라 그 시점에 수치가 준비돼 있고, 작업트리 미커밋 코드에 전투를 멈추는 인터미션 패턴이 이미 있어 재사용한다. 이것이 '첫 관찰 30초 이내'를 플레이어 실력과 무관하게 보장하는 수단이다 — 수동 사격 전환으로 웨이브 1 소요가 길어지므로 웨이브 종료에 매달면 못하는 사람일수록 핵심을 못 본다.
- [must] req-restore-procedure: 아레나 회수 절차를 순서대로 고정한다. (1) 작업트리 미커밋 82줄을 별도 브랜치에 먼저 커밋해 보존한다 — 지금 어디에도 저장돼 있지 않아 잘못 되돌리면 사라진다. (2) 8d3a85f에서 가져올 파일을 목록으로 고정한다(게임 씬 2종·적·업그레이드·변주·웨이브 실행·텔레메트리 수집기·인터벌 UI·디렉터 폴백/리포트/검증기와 그 테스트). (3) **`src/contracts/directive.ts`는 회수 대상에서 제외하고 현행 버전을 유지한다** — 두 시점 사이 차이는 선택 필드 두 개뿐이고 LLM 출력 규격은 동일하므로, 계약 파일만 현행으로 두면 계약 동결이 자동 성립하고 **프록시 재배포가 아예 불필요해진다**. (4) 회수 직후 `bash scripts/ai_harness.sh --fast`가 PASS인 것을 다음 작업의 전제 조건으로 건다.
- [must] req-manual-fire: 자동 조준·자동 사격을 제거하고 마우스 조준 + 수동 발사로 전환한다. 플레이어가 빗나갈 수 있어야 하며 이것이 유일한 실패 축 복원 수단이다. **부족함은 발사 쿨다운·정확도로만 만들고 탄약 자원과 신규 업그레이드 카드는 추가하지 않는다** — 업그레이드 카드 목록과 계약의 봉인 대상 목록은 항목·순서가 일치해야 하고 테스트가 그것을 강제하므로, 카드를 추가하면 계약이 바뀌고 금지된 프록시 재배포가 강제된다. 기존 카드 3종(연사·다중 발사·탄속)은 자동 사격 전제로 값이 매겨져 있으므로 수동 사격 기준으로 값만 재조정한다(카드 목록 자체는 불변).
- [must] req-observation-text: 관찰 문장의 수치 부분은 새로 만들지 않고 **현행 코드에 이미 살아 있는 습관 판정 모듈(`src/game/habits.ts`)이 생성하는 근거 문자열**('한 사분면 체류 82%')을 그대로 쓴다. 페르소나가 실린 문장(예고·도발)은 계약에 이미 있는 두 문자열 필드(60자·100자)에 실어 계약을 한 글자도 바꾸지 않고 화면에 올린다. 이 모듈은 8d3a85f에서 꺼내올 대상이 아니며 현행 씬이 이미 호출해 판정하고 미터를 그리고 있다.
- [must] req-multiplier: 배수(multiplier)를 새로 만든다. 습관 판정 모듈에는 판정 세 값(적중·깨짐·무효)과 미터 채움 비율만 있고 배수 개념 자체가 없다. 예고를 깨면 배수가 오르고 적중당하면 내려가며, 화면에 상시 표시되고 웨이브 종료 시 정산으로 확정된다. 이것이 승제가 진단축으로 세운 '비트에 정산이 있는가'의 실행부다.
- [must] req-causal-redeploy: 예고한 방향과 실제 스폰이 반드시 일치해야 한다. 계약의 스폰 위치 값에는 동서남북 네 방향 외에 원형·협공·배후 셋이 더 있고 뒤 셋은 플레이어 위치 기준이라 예고한 화면 편향 방향과 어긋날 수 있다. **관찰이 활성인 웨이브에서는 스폰 위치를 네 방향으로 제한하고, 예고 방향과 다른 값이 오면 엔진이 예고 방향으로 강제 보정한다.** LLM이 죽어도(req-llm-fallback) 엔진이 관찰된 편향에서 방향을 결정론으로 골라 예고와 스폰의 인과가 유지된다.
- [must] req-local-memory: 집단 기억을 2단으로 만들고 **1단(브라우저 로컬 기억)을 must로 한다.** 브라우저 저장소에 이 사람의 이전 런 요약을 남기고 '너는 아까도 바로 여기서 죽었다', '3판째 같은 쪽으로 돈다'를 만든다. 심사자는 대개 1~2판만 하므로 교차 플레이어 통계보다 이쪽이 훨씬 확실하게 발동하고 더 개인적이라 더 무섭다. 개인식별정보는 저장하지 않는다.
- [should] req-shared-memory: 2단(교차 플레이어 누적 기억)은 should로 내린다. 리포지토리 확인 결과 인프라가 통째로 없다 — Supabase DB 클라이언트 의존성 없음, 소스 참조 0건, 마이그레이션 디렉터리 없음, 배포 워크플로가 넣어 주는 빌드 변수는 디렉터 주소 하나뿐. 필요한 것은 테이블 스키마·익명 쓰기 권한 정책·신규 빌드 변수·워크플로 수정이고 **콘솔 작업은 승제만 할 수 있다.** 조회 실패·미설정 시 화면은 1단 로컬 기억만으로 정상 동작해야 하며 오류를 노출하지 않는다. 리포지토리가 public이므로 쓰기 경로에는 서버측 스키마 검증과 행 수 상한을 건다. 표시 문구는 실제 누적값에 맞춘다 — 시드로 수를 부풀리지 않는다('이 자리에서 죽은 3번째'도 성립한다).
- [must] req-verdict-void-rule: 판정과 재배치가 서로를 무효화하지 않게 규칙을 고정한다. 습관 판정은 디렉터가 지표를 강제한 웨이브를 무효 처리하도록 돼 있는데, 예고 방향으로 재배치하는 것 자체가 플레이어를 그 방향에서 밀어내는 행위다. **재배치는 무효 사유로 보지 않는다**(재배치를 피하는 것이 곧 플레이어의 수이므로) — 무효는 기존 변주(용암 등)가 지표를 직접 강제하는 경우로만 한정하고, 그 판정 근거를 화면에 명시한다.
- [must] req-llm-fallback: LLM 프록시가 죽어도 게임이 처음부터 끝까지 100% 성립해야 한다. 관찰·예고 문장은 폴백 뱅크로 대체하되 수치는 엔진 실측값을 채워 넣어 문장이 거짓이 되지 않게 하고, 예고 방향은 엔진이 결정론으로 정해 인과를 유지한다(req-causal-redeploy).
- [must] req-video-on-pages: 30~60초 영상은 **반드시 배포된 GitHub Pages 빌드에서 촬영한다.** 로컬 개발 환경은 CORS로 LLM 경로가 차단되므로 로컬에서 찍으면 관찰·예고 문장이 전부 폴백 문구로 찍히고, 가장 많이 보일 산출물에서 차별점이 통째로 사라진다. 교차 기억 수치도 빌드 변수 때문에 배포본에서만 실값이 나온다. 촬영 전 director 호출 200과 LLM 문장 표시를 육안 확인한다.
- [must] req-gate-criteria: 13:00 게이트의 통과 기준을 체크리스트로 고정한다 — 배포된 Pages에서 1런을 돌려 ① 마우스로 조준·발사되고 빗나갈 수 있다 ② 웨이브 1의 12초 지점에 첫 관찰이 화면 중앙에 뜬다 ③ 관찰 1.2초 뒤 예고가 뜨고 그 시점엔 아직 적이 안 나온다 ④ 다음 웨이브에서 예고한 방향에 마커가 먼저 그려지고 그 자리에서 적이 나온다 ⑤ 하네스 PASS. 다섯 항목 전부 ✅여야 통과다. 하나라도 ❌면 req-cut-order를 발동한다.
- [must] req-cut-order: 시간이 모자랄 때 자르는 순서를 고정한다 — ① 교차 플레이어 기억(req-shared-memory) ② 역이용 보상 연출(req-counterplay-reward) ③ 사망 화면 '너에게서 배운 것'(req-death-learned) ④ 히트맵 오버레이 ⑤ 판정 근거 상세 표시. **절대 자르지 않는 것**: 수동 사격, 5비트 소름 순서(관찰→예고→마커→스폰→확인), 12초 첫 관찰, 예고와 스폰의 인과, 배수 정산, 하네스 PASS, 제출 5종. **최저선**: 예고 없이 관찰만 있는 빌드는 제출하지 않는다 — 예고가 이 게임의 존재 이유이므로, 거기까지 못 가면 관찰·예고를 스크립트(엔진 결정론)로 고정해서라도 5비트를 성립시킨다.
- [must] req-submission-five: 제출 5종을 완주한다 — 플레이 가능한 빌드+전체 소스, 30~60초 실플레이 영상, 게임소개 PDF, AI활용기술문서 PDF(1인이므로 팀원 롤 기술서 생략), 신청 폼 제출. 하나라도 누락되면 심사 제외다. 기존 PDF 2종은 개정 전 아레나(자동 사격·전멸 클리어)를 서술하므로 게임 서술부를 다시 쓴다.
- [should] req-docs-early: 제출 문서 2종에서 게임과 무관한 절(AI 2층 구조·권한 경계·24시간 4회 전환의 판단 기록)은 동결을 기다리지 않고 오전 대기 시간에 먼저 쓴다. 동결 후에는 스크린샷과 게임 서술부만 갈아끼운다. 커밋 기록 유지가 제출 요구사항이라 전환 이력은 심사자에게 노출되므로 숨기지 말고 판단 기록으로 서술한다.
- [should] req-counterplay-reward: 예고를 역이용한 통과(일부러 편향을 만들어 예고를 유도한 뒤 반대편으로 빠지기)가 1판 안에 최소 1회 가능해야 하고 성공 시 배수 보상과 화면 연출로 드러나야 한다.
- [should] req-death-learned: 사망·종료 화면에 '너에게서 배운 것' 한 줄과 기억 내 위치(로컬 우선, 교차 기억이 있으면 함께)를 표시한다. 60초 영상의 마지막 비트다.
- [could] req-salvage-two-pieces: 미커밋 82줄을 버리기 전에 두 조각을 발췌한다 — 적이 플레이어 이동을 예측해 앞을 쏘는 계산과, 전투를 멈추는 인터미션 패턴. 앞의 것은 수동 사격으로 플레이어가 느려진 만큼 '얘가 나를 읽는다'는 체감을 싸게 준다. 뒤의 것은 req-first-observation-midwave가 이미 쓴다.
- [should] req-record-decision: 이번 방향 결정을 오늘 안에 vault 선택 기록으로 남긴다(선택 / 기각 대안 / 이유 / 패턴). 특히 두 가지를 명시한다 — ① 8/8 확정 '구작 아레나 제출 폐기(배수진)'를 2026-08-10 승제가 명시적으로 뒤집었고 근거는 '아레나가 곧 내 추억 게임'이라는 본인 판단이라는 것 ② 8/8 밤 세 번의 전환이 기록 없이 일어난 것이 이번 사건의 구조였다는 것. 기록 없는 방향 결정이 24시간에 네 번 뒤집힌 것이 이 프로젝트의 실패 형태다.
- [could] req-spend-cap: Anthropic 콘솔 워크스페이스 지출 한도를 확인·설정한다. 프록시가 무인증으로 공개돼 있고 코드의 상한 2종은 우회 가능하므로 실효 하드캡은 이것뿐이다. 코드 변경·재배포 불필요.

### 데이터 계약
- dc-directive: Supabase Edge Function director (claude-haiku-4-5) → 게임 클라이언트 디렉터 층 (schema: src/contracts/directive.ts DIRECTIVE_JSON_SCHEMA — 현행 버전 유지·동결. 회수 대상에서 제외하므로 프록시 재배포 불필요)
- dc-telemetry: 게임 엔진 텔레메트리 수집기 + habits.ts 근거 문자열 → Edge Function director(관찰 근거) 및 화면 관찰 문장 (schema: src/game/collector.ts 런 요약 + src/game/habits.ts evidence() (비개인정보만))
- dc-local-memory: 게임 클라이언트 런 종료 시 → 같은 브라우저의 다음 런 (schema: 신규 — 브라우저 저장소 런 요약(개인식별정보 0))
- dc-shared-memory: 게임 클라이언트 런 종료 시 (should — 미구현 시 경로 자체가 없음) → Supabase Postgres 누적 테이블 + 조회 (schema: 신규 — 익명 런 요약 1행. 서버측 스키마 검증·행 수 상한. IP·핑거프린트 저장 금지. 미설정·실패 시 로컬 기억만으로 정상 동작)

## 4. 제외 범위
- 감옥 스텔스 자산 이식(시야 콘·발각 게이지·섹터 맵·프롭 드로잉) — 전부 폐기한다
- 계약·거래 시스템, 심리전 이지선다, 갤러그형 편대 슈팅 등 이번 심의에서 검토했다 기각된 대안
- 신규 업그레이드 카드 추가 및 계약 봉인 목록 변경 — 계약 충돌을 일으킨다
- 디렉티브 계약 스키마 변경 및 그에 따른 프록시 재배포
- 신규 씬 추가, 사운드 추가, 칭호 다양화, 보스, 신규 적 타입, 외부 에셋 도입
- 밸런스 전면 재설계 — 판정 미달 시 레버는 소름 5비트의 체류 시간(1.2초·0.6초)과 상수 1개까지만

## 5. 핵심 결정 사항
- 채택안: opt-new-game-gated (sj-team 심의로 확정 — 상세 근거는 run의 decision 산출물, source_decision_hash 참조)

## 6. 구현 체크포인트
- [ ] (/dlc Phase 2에서 분해 — 체크포인트 분해 권한을 /dlc에 위임함, sj-team 승인으로 갈음)

## 7. 검증 계획
완료 기준(success criteria):
- sc-first-observation: 첫 관찰 문장이 화면 중앙에 등장하는 시각 → 게임 시작 후 30초 이내 (설계상 12초 지점 고정) (검증: 배포본 실플레이 스톱워치 3런, 전 런 30초 이내)
- sc-warning-before-spawn: 예고 표시 시점과 해당 웨이브 첫 적 스폰 시점의 선후 → 예고가 항상 먼저이고 그 사이 간격 1.2초 이상, 스폰 마커가 적보다 0.6초 이상 먼저 (검증: 화면 녹화 프레임 대조 3회 + 타이밍 로그)
- sc-causal-redeploy: 예고한 방향과 그 웨이브 실제 스폰 방향의 일치율 → 관찰이 활성인 웨이브에서 100% 일치 (엔진 강제 보정으로 보장) (검증: 스폰 좌표 로그와 예고 방향 값을 웨이브별로 대조, 불일치 0건)
- sc-observation-grounded: 관찰 문장에 포함된 실측 수치와 텔레메트리 실제값의 일치 → 전건 일치 — 지어낸 수치 0 (검증: 화면 문장과 콘솔 텔레메트리 값 대조 3건)
- sc-miss-possible: 플레이어 명중률 → 100% 미만 — 빗나갈 수 있음이 실측으로 확인 (검증: 텔레메트리 명중/발사 비율, 실플레이 2런)
- sc-multiplier-settled: 배수 표시와 웨이브 종료 정산 → 배수가 상시 표시되고 판정(적중/깨짐)에 따라 변하며 종료 시 확정 (검증: 실플레이 1런 전 웨이브 육안 확인)
- sc-local-memory: 같은 브라우저 2런째에 로컬 기억 문장 발동 → 2런째에 이전 런을 지목하는 문장이 최소 1회 표시 (검증: 연속 2런 실플레이)
- sc-llm-dead-ok: 프록시 차단 상태에서 완주 및 인과 유지 → 게임 완주 + 예고와 스폰 인과 유지 + 수치가 실측값 (검증: 로컬 dev(CORS 차단 = 폴백 경로) 1런 완주)
- sc-harness: 하네스 결과 → HARNESS RESULT: PASS (검증: bash scripts/ai_harness.sh --fast)
- sc-gate-1300: 13:00 게이트 체크리스트 5항 → 5항 전부 ✅ (검증: 배포본 1런으로 req-gate-criteria 항목별 대조)
- sc-video-source: 영상 촬영 환경 → 배포된 Pages 빌드에서 촬영, LLM 문장이 폴백이 아님 (검증: 촬영 직전 director 호출 200 확인 + 영상 내 문장이 실측 수치 포함)
- sc-submission: 제출 5종 완주 → 빌드+소스·영상·소개 PDF·기술문서 PDF·폼 제출 전부 완료 (검증: 제출 폼 완료 화면 + 각 산출물 파일 존재 확인)

- 체크포인트마다: `ai_harness.sh --fast`
- 하네스 부재 시 폴백 명령: (/dlc가 대상 프로젝트에서 확정)
- 최종: `ai_harness.sh --full`

## 8. 리스크
- [critical] risk-time: 게임 작업이 16:30을 넘기면 잘리는 것은 게임이 아니라 산출물이고, 5종 중 하나라도 누락되면 심사 제외다. 폴백 제출물이 없다. — 완화: 13:00 게이트(체크리스트 5항)와 16:30 동결을 하드 규칙으로 운영하고, 미달 시 req-cut-order의 순서대로 자른다. 문서의 게임 무관 절은 오전에 선행 작성한다(req-docs-early).
- [high] risk-no-goosebumps: 관찰과 재배치를 구현해도 플레이어가 '얘 나 노린다'를 못 느낄 수 있다. 이 게임의 존재 이유가 그 한 감정이라 실패 시 대체 수단이 없다. — 완화: 소름을 연출 순서 문제로 환원한다(req-goosebump-sequence). 판정은 sc-warning-before-spawn·sc-causal-redeploy·sc-first-observation으로 반증 가능하게 하고, 미달 시 손대는 레버를 체류 시간 두 개로 한정한다.
- [high] risk-restore-breaks-build: 회수 시 파일 조합이 어긋나면 타입 체크가 무더기로 깨져 오전을 통째로 먹는다. 미커밋 82줄은 어디에도 저장돼 있지 않아 잘못 되돌리면 사라진다. — 완화: req-restore-procedure의 4단계를 순서대로 지키고, 회수 직후 하네스 PASS를 다음 작업의 전제 조건으로 건다. 계약 파일은 회수 대상에서 제외한다.
- [high] risk-silent-fallback: 프록시 문제·캡 소진 시 화면은 정상이고 테스트도 통과하는데 관찰·예고 문장만 폴백 고정 문자열로 바뀐다. 특히 영상에 폴백이 찍히면 가장 많이 보일 산출물에서 차별점이 사라진다. — 완화: 영상은 배포본에서만 촬영하고(req-video-on-pages) 촬영 직전·제출 직전 director 200과 LLM 문장을 육안 확인한다. 2026-08-10 06:5x 확인 결과 현재 정상이다.
- [high] risk-terms-unchecked: 재직자 보증·약관(참가약관 제2조) 확인이 세 개 스펙 문서 모두에서 미해결이다. 게임 방향과 완전히 독립인데 여기서 막히면 모든 작업이 제출되지 못한다. — 완화: 승제가 게임 작업과 병렬로 오전 중 종결하고 스펙 체크박스에 반영한다.
- [medium] risk-shared-memory-console: 교차 기억은 콘솔 작업(테이블·권한 정책·빌드 변수)이 선행돼야 하고 승제만 할 수 있다. 하나라도 빠지면 배포본에서만 조용히 죽는다. — 완화: 교차 기억을 should로 내리고 로컬 기억(must)만으로 게임이 성립하게 만든다. 조회 실패 시 오류를 노출하지 않고 로컬 기억으로 대체한다.
- [high] risk-doc-drift: 기존 제출 PDF 2종은 개정 전 아레나(자동 사격·전멸 클리어)를 서술하므로 그대로 내면 심사자가 설명과 다른 게임을 먼저 만난다. — 완화: 게임 무관 절은 오전에 먼저 쓰고(req-docs-early), 동결 후 게임 서술부와 스크린샷만 갈아끼운다.

### 제약
- 마감 2026-08-11 00:00 KST (공고 사이트 카운트다운 직접 관측으로 확정)
- 13:00 시각 게이트(req-gate-criteria 5항), 게임 동결 16:30, 이후 게임 변경은 영상·문서를 다시 만들 수 있을 때만
- 좌표·수치는 100% 엔진 계산, LLM은 enum과 짧은 문장만 산출한다
- 계약 파일을 회수 대상에서 제외해 계약 동결을 성립시키므로 프록시 재배포는 하지 않는다. 부득이한 경우에만 supabase functions deploy director --project-ref rffpffpjnggpqpvvzqsv --no-verify-jwt (플래그 누락 시 401)
- 로컬 dev는 CORS로 LLM 경로가 차단되므로 LLM 인게임 확인과 영상 촬영은 배포된 Pages에서만 가능
- GitHub 리포지토리는 public이며 커밋 기록을 유지해야 한다 — 비밀값 커밋 금지
- 기억에 개인식별정보를 저장하지 않으며 '플레이가 기록된다'는 사실을 화면에 명시한다
- ~~1인 참가~~ → **3인 팀 참가로 확정(2026-08-10 제출 당일)**: 지승제(팀대표)·유한결·이해진. 팀 소개 문서 제출함
- 교차 기억(req-shared-memory)의 Supabase 콘솔 작업(테이블·익명 권한 정책·빌드 변수 등록)은 승제만 수행 가능하며 AI가 대신할 수 없다
- 8/8 확정 '구작 아레나 제출 폐기(배수진)'를 승제가 2026-08-10 명시적으로 뒤집었다 — 근거는 아레나가 곧 승제의 추억 게임이라는 본인 판단

## 9. ui_direction
existing_ui

## 10. 승인 상태
- [x] 사용자 승인 (sj-team run nan-2026-2026-08-11-00-0-eb8ad4 approve-spec 승계 — 해시 바인딩 승인)
- 승인일: 2026-08-09
- 승인자: byseungje
- source spec_input_hash: sha256:dc45109beeb4a67fa4a50fdfb02b493bd9d184a85b8dd7614b356c7b9777bb18

## 진행 상태  (에이전트 관리 — 사용자 본문 아님)
- [x] 스펙 frozen (sj-team 승계)
- [ ] 체크포인트 분해 완료
- [ ] CP1 … CPn 진행 체크
- [ ] 최종 --full 통과
- 마지막 업데이트: 2026-08-09
